# ── Postgres (used by docker-compose) ─────────────────────────────────────────
DB_NAME=cio_dashboard
DB_USER=cio_user
DB_PASSWORD=change-this-strong-password

# ── Frontend port (host port that nginx listens on) ───────────────────────────
FRONTEND_PORT=80
